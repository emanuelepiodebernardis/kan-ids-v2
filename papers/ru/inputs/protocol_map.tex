\begin{table}[!htbp]
\caption{Шесть отдельных аппаратных протоколов. Основное сравнение размещения C3 --- факторный опыт; HW500 характеризует энергию на общих 500 потоках, RAM500 --- компоненты памяти. Строки не объединяются в одну совокупность измерений. C3 работает на 160~МГц; размещение относится к ядру и параметрам модели.}
\label{tab:protocol-map}
\centering\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabular}{>{\RaggedRight\arraybackslash}p{2.1cm}>{\RaggedRight\arraybackslash}p{3.2cm}>{\RaggedRight\arraybackslash}p{2.4cm}>{\RaggedRight\arraybackslash}p{3.1cm}>{\RaggedRight\arraybackslash}p{4.0cm}}
\toprule
Опыт & Измеряемая граница & Код / параметры & Входы и повторы & Интерпретация\\
\midrule
Исходные пять моделей (прил.~\ref{app:historical-hardware}) & \texttt{micros()}, шаг 1~мкс; один вызов до бинарного решения & Исходное размещение; параметры во Flash & 500 потоков; пять проходов со сбросами на модель & 64 вызова прогрева; копирование входа, serial и сверка вне интервала. LUT на C3 медленнее на 7.58\%.\\
Диагностика (прил.~\ref{app:historical-hardware}) & Счётчик циклов; логит или блок из 32 вызовов с циклом и сохранением & IRAM / Flash или DRAM & 500 потоков; пять проходов в одной загрузке & 2,000 вызовов прогрева; сбалансированный порядок; \texttt{same32} повторяет один вход.\\
Основной факторный (разд.~\ref{sec:c3followup}) & Счётчик циклов; вызов и бинарный порог & Flash или IRAM / Flash или DRAM & 500 потоков; восемь проходов со сбросами, восемь условий & Общая граница измерения; порядок Williams; прерывания разрешены. LUT Flash/DRAM быстрее в $1.706$ раза.\\
USB-пилот (прил.~\ref{sec:energy-pilot}) & Длительность партии / число вызовов; мощность центрального окна & Параметры во Flash; отдельные приложения & 20 потоков повторяются; две записи на модель и плату & Около 120~с; входы в RAM; цикл и checksum включены. Исторический пилот.\\
HW500 (разд.~\ref{sec:hw500}) & Длительность полной партии и интеграл USB-мощности того же интервала / число вызовов & Модель и входы во Flash & Общие 500 потоков; пять записей на модель и плату & Включены загрузка входа, цикл, checksum и служебное обслуживание. Одна непрерывная запись прибора на плату.\\
RAM500 (разд.~\ref{sec:ram500}) & Статические области; watermark стека и показатели heap & Модель и входы во Flash & 500 потоков по три прохода; три свежие загрузки на модель и плату & Mega: рабочая область со сверкой; C3: история задачи. Положительные контроли после baseline; не единый Peak RAM.\\
\bottomrule
\end{tabular}
\end{table}

Журнал загрузки факторного опыта задаёт DIO, 80~МГц и 4~МБ Flash; это конфигурация, а не независимое измерение эффективной частоты шины. Сборка использует 160~МГц CPU, \texttt{-Os}, отключённые LTO, jump tables и switch conversion. Архивного \texttt{sdkconfig} нет, поэтому параметры строк кеша и тиков RTOS не приписываются измеренной системе. Промахи кеша не измерялись; runtime-режим не выводится только из заголовка BIN.
