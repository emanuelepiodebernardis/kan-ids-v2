\begin{table}[!htbp]
\caption{Четыре различных протокола ESP32-C3. Одна физическая плата, 160~МГц, подготовленные исходные признаки TON\_IoT; строки не объединяются. Размещение относится к ядру и параметрам модели.}
\label{tab:protocol-map}
\centering\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabular}{p{2.15cm}p{3.25cm}p{2.6cm}p{3.2cm}p{4.25cm}}
\toprule
Опыт & Таймер и граница & Код / параметры & Входы и повторы & Прогрев и интерпретация\\
\midrule
Исходные пять моделей (прил.~\ref{app:historical-hardware}) & \texttt{micros()}, шаг 1~мкс; один вызов до бинарного решения & Исходное размещение; параметры во Flash & 500 потоков; пять проходов со сбросами на модель & 64 вызова прогрева; копирование входа, serial и сверка вне интервала. LUT медленнее на 7.58\%.\\
Диагностика (прил.~\ref{app:historical-hardware}) & Счётчик циклов; логит или блок из 32 вызовов с циклом и сохранением & IRAM / Flash или DRAM & 500 потоков; пять проходов в одной загрузке & 2,000 вызовов прогрева на блок, сбалансированный порядок; прерывания разрешены. \texttt{same32} повторяет один вход.\\
Основной факторный (разд.~\ref{sec:c3followup}) & Счётчик циклов; вызов и бинарный порог & Flash или IRAM / Flash или DRAM & 500 потоков; восемь проходов со сбросами, восемь условий & Прогрев на всех 500 входах; порядок Williams; прерывания разрешены; serial вне блоков; \texttt{-Os}, без LTO. Ускорение LUT Flash/DRAM: $1.706\times$.\\
USB-пилот (разд.~\ref{sec:energy-pilot}) & Длительность партии MCU / число вызовов; USB-мощность центрального окна & Параметры во Flash; отдельные приложения & Первые 20 потоков повторяются; две записи на модель в одной загрузке C3 & Прогрев и калибровка числа вызовов; около 120~с; цикл, индексы, checksum, прерывания включены. Wi-Fi/Bluetooth не инициализированы; без явного yield. Отношение времени: $1.567\times$.\\
\bottomrule
\end{tabular}
\end{table}

Журнал загрузки факторного опыта задаёт DIO, 80~МГц и 4~МБ Flash; это конфигурация, а не независимое измерение эффективной частоты шины. Сборка использует 160~МГц CPU, \texttt{-Os}, отключённые LTO, jump tables и switch conversion. Архивного \texttt{sdkconfig} нет, поэтому параметры строк кеша и тиков RTOS не приписываются измеренной системе. Промахи кеша не измерялись; runtime-режим не выводится только из заголовка BIN.
