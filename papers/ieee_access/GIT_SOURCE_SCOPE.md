# Git source export v0.12

Reviewed manuscript sources, fonts, input figures, numerical evidence and build/check scripts. Compiled main.pdf, temporary TeX files, page renders and build logs belong to the separate source/PDF release. Verification JSON receipts identify that release and do not claim a PDF is committed. MANIFEST.json lists only this Git source export; figure PDFs remain vector source assets. Build with python scripts/build_paper.py. Original review receipts remain historical and labeled. The Git export removes only excess trailing blank lines from ABSTRACT_EN.txt, inputs/ml_supplement_tables.tex and inputs/historical_hardware.tex where present; these whitespace-only changes preserve TeX and document content. Read the manuscript README for scientific scope and proposed author-order status.
