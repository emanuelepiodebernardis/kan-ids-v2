/* Wire.h — stub per compile-check HOST del blocco ENABLE_INA219 (offline).
 * Il framework Arduino reale fornisce la versione vera. Wire e' gia'
 * definito in arduino_stub.h; qui basta esistere come header. */
#pragma once
